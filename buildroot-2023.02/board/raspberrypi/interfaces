auto lo
iface lo inet loopback

#auto eth0
#iface eth0 inet dhcp
#	pre-up /etc/network/nfs_check
#	wait-delay 15

auto wlan0
iface wlan0 inet dhcp
	pre-up wpa_supplicant -D nl80211 -i wlan0 -c /etc/network/wpa_supplicant.conf -B
	wait-delay 5

iface default inet dhcp
